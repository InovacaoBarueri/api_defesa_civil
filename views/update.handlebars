<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Alert</title>
</head>
<body>
  <h1>Update Alert</h1>
  <form action="/update" method="POST">
    <input type="hidden" name="id" value="{{alert.id}}">

    <label for="title">Title:</label>
    <input type="text" name="title" id="title" value="{{alert.title}}">

    <label for="subtitle">Subtitle:</label>
    <input type="text" name="subtitle" id="subtitle" value="{{alert.subtitle}}">

    <label for="content">Content:</label>
    <textarea name="content" id="content">{{alert.content}}</textarea>

    <label for="risk">Risk Level:</label>
    <select name="risk" id="risk">
      <option value="low" {{#ifEqual alert.risk "low"}}selected{{/ifEqual}}>Low</option>
      <option value="medium" {{#ifEqual alert.risk "medium"}}selected{{/ifEqual}}>Medium</option>
      <option value="high" {{#ifEqual alert.risk "high"}}selected{{/ifEqual}}>High</option>
    </select>

    <label for="affectedRegions">Affected Regions:</label>ext" name="affectedRegions" id="affectedRegions" value="{{alert.affectedRegions}}">

    <button type="submit">Update Alert</button>
  </form>
</body>
</html>
